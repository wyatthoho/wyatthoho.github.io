# frozen_string_literal: true

source "https://rubygems.org"

gem 'jekyll', '~> 4.4', '>= 4.4.1'

# Windows has no IANA timezone database, so Jekyll's `timezone` config
# needs these to resolve time zones at build time.
gem 'tzinfo', '~> 2.0'
gem 'tzinfo-data'
